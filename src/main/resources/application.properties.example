spring.application.name=server

# 기본 프로파일 설정
# dev: 개발 환경
# test: 테스트 환경  
# prod: 운영 환경
spring.profiles.active=test

# 주의: 이 파일을 복사해서 application.properties로 만들고
# 필요한 설정을 추가하세요.
# application.properties는 .gitignore에 포함되어 있어 Git에 올라가지 않습니다.

